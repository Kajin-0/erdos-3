# Third-generation recursive frontier

## Status

Exact finite theorem for the certified `local37` retained transition.

The computation uses the same fixed objects as the preceding retained-child results:

- the `local37` first-generation policy;
- the unique 21-state point-disjoint first retained family;
- lexicographic coordinated deletion on retained recursive states;
- global exact-state quotienting;
- componentwise maximum-harmonic independent-set selection in the same-shell conflict graph;
- and original `S_7` root provenance propagated pointwise.

**Verifier:** `src/verify_third_generation_recursive_frontier.py`.

**Certificate:** `data/third_generation_recursive_frontier_certificate_2026-07-13.txt`.

Certificate SHA-256:

```text
efdd41c014104f328f28c3d13b097335fd2b1730859b74134344329251b135d0
```

---

## 1. Exact third-generation quotient

Only the 14 recursive second-generation retained states are propagated. They contain 7,882 points.

Their complete lexicographic resolutions have:

```text
selected actions = 6,858
terminal residual points = 1,024
raw shell occurrences = 474
raw occurrence points = 14,639
exact state classes = 108
same-shell conflict edges = 386
conflict components = 29
largest component = 15
DP states examined = 137.
```

Every conflict component has a unique optimum. The resulting third retained family is point-disjoint and has:

| output type | states | points |
|---|---:|---:|
| terminal, three-term-progression-free | 18 | 110 |
| recursive | 14 | 4,789 |
| total | 32 | 4,899 |

Thus the retained-state count remains controlled, but harmonic mass does not.

---

## 2. The two-generation contraction does not iterate

Let

```math
H_1=\text{first retained harmonic mass},
```

```math
H_2^{\mathrm{rec}}=\text{second-generation recursive retained mass},
```

and let `H_3`, `H_3^{term}`, and `H_3^{rec}` denote the total, terminal, and recursive third-generation retained masses.

Exact arithmetic gives

```math
9.497798
<
\frac{H_3}{H_2^{\mathrm{rec}}}
<
9.497799,
```

with terminal share

```math
0.788208
<
\frac{H_3^{\mathrm{term}}}{H_3}
<
0.788209.
```

Most importantly,

```math
\boxed{
2.011553
<
\frac{H_3^{\mathrm{rec}}}{H_2^{\mathrm{rec}}}
<
2.011554.
}
```

The recursively continuing output therefore expands by slightly more than 101.1553% relative to the preceding recursive generation.

It also satisfies

```math
\boxed{
1.886248
<
\frac{H_3^{\mathrm{rec}}}{H_1}
<
1.886249.
}
```

Consequently, the strict row

```math
\frac{31}{500}H_1+H_2^{\mathrm{rec}}<H_1
```

is a valid one-transition statement but is not an iterating Bellman potential under the same fixed policy and retention rule.

This does not rule out a larger state-dependent or multi-coordinate potential. It rules out treating the recorded two-generation contraction coefficient as a stable recursive invariant.

---

## 3. Terminal-token recreation

The 43 second-generation terminal tokens were previously represented by

```math
\tau(u)=(u,p),
```

where `u` is the terminal descendant label and `p` is original `S_7` root provenance.

They have no collision with any first-generation raw or retained token. At the third generation, however, exactly one token recurs:

```text
(u,p) = (60, 1,354,490).
```

Both occurrences are terminal singleton states generated by a step-5 middle fiber:

| generation | immediate provenance | source | source step |
|---:|---:|---|---:|
| 2 | 2,810 | middle fiber | 5 |
| 3 | 440 | middle fiber | 5 |

Therefore:

- `(u,p)` is not collision-sound across these generations;
- `(u,p,source,source_step)` is also insufficient;
- `(u,p,immediate provenance)` separates the recorded collision;
- no second-generation terminal `(u,p)` token recurs inside the third-generation recursive family.

Immediate provenance is a viable next signature component on this finite transition. No global injectivity theorem is claimed.

---

## 4. Numerical recurrence is much more common

Ignoring provenance, 28 of the 43 earlier terminal numerical labels reappear in the third retained family:

```text
17 in terminal output
11 in recursive output.
```

Seven complete earlier terminal numerical states regenerate exactly:

```text
{1}
{5}
{10}
{60}
{61,62}
{122,123}
{147,152,153}
```

Thus numerical identity alone is far too coarse for a global sink ledger. Root provenance removes nearly all observed collisions, but not all of them.

Third-generation root provenance has:

```text
4,840 distinct root labels
59 repeated root labels
maximum multiplicity 2.
```

The mass expansion is therefore not explained by high point multiplicity alone.

---

## 5. Consequences for the active program

The previous candidate architecture

```text
recursive contraction + first-appearance terminal ledger
```

requires two corrections:

1. recursive contraction is not monotone from one retained generation to the next;
2. terminal identity requires at least one additional path-sensitive coordinate beyond `(u,p)`.

The next viable state must retain enough information to distinguish:

```text
current label u
root provenance p
immediate provenance or parent-path signature
terminal versus recursive status
retained-generation scale.
```

The immediate next test is to search for a finite weighted potential on these coordinates that absorbs both the second-generation contraction and third-generation expansion. If no such exact rational potential exists, the smallest infeasible subsystem should be exported as the next no-go theorem.

This result is fixed-policy, fixed-retention, and finite. It is not a universal theorem over all coordinated deletion schedules or all retained quotients.
