# Generation-aware retained potentials

## Status

Exact finite theorem for the certified first, second-recursive, and third-recursive retained families generated by the fixed `local37` policy, lexicographic recursive propagation, and the global point-disjoint retained quotient.

The third generation disproves monotonicity of raw recursive harmonic mass:

```math
2.011553
<
\frac{H_3^{\mathrm{rec}}}{H_2^{\mathrm{rec}}}
<
2.011554.
```

This note records two provenance-aware coordinates that nevertheless decrease across both observed recursive transitions.

**Verifier:** `src/verify_generation_aware_retained_potentials.py`.

**Certificate:** `data/generation_aware_retained_potentials_certificate_2026-07-13.txt`.

Certificate SHA-256:

```text
0e1b81e3562990e6071db64c4d6544aab1bb0c78aaae08eee780f3f9d6f81063
```

---

## 1. Retained families

The three exact recursive accounting levels are:

| family | states | points |
|---|---:|---:|
| first retained family | 21 | 11,753 |
| second-generation recursive family | 14 | 7,882 |
| third-generation recursive family | 14 | 4,789 |

For every retained point, let

```text
u = current retained label
p = original S7 root provenance
i = immediate provenance in the preceding retained family.
```

Let

```math
H_g=\sum_{u\in F_g}\frac1u.
```

---

## 2. Repeated-root descendant potential

Let `m_g(p)` be the multiplicity of root provenance label `p` among the points of retained recursive family `F_g`. Define

```math
R_g
=
\sum_{(u,p)\in F_g\,:\,m_g(p)>1}\frac1u.
```

Thus `R_g` is harmonic mass at the **current descendant labels**, restricted to points whose original root provenance is reused somewhere else in the same retained generation.

The candidate potential is

```math
\boxed{
\Phi_g^{\mathrm{rep}}=H_g+2R_g.
}
```

Its approximate exact values are

| family | `H + 2R` |
|---|---:|
| first retained | 0.831320413049 |
| second recursive | 0.602952934675 |
| third recursive | 0.566439976241 |

The exact ratios satisfy

```math
\frac{145059}{200000}
<
\frac{\Phi_2^{\mathrm{rep}}}{\Phi_1^{\mathrm{rep}}}
<
\frac{45331}{62500},
```

and

```math
\boxed{
\frac{939443}{1000000}
<
\frac{\Phi_3^{\mathrm{rep}}}{\Phi_2^{\mathrm{rep}}}
<
\frac{234861}{250000}.
}
```

Therefore the same potential contracts by

```text
27.4704%–27.4705% on the first transition
6.0556%–6.0557% on the second transition.
```

Across both transitions,

```math
\frac{681373}{1000000}
<
\frac{\Phi_3^{\mathrm{rep}}}{\Phi_1^{\mathrm{rep}}}
<
\frac{340687}{500000}.
```

This is the stronger of the two finite witnesses.

### Mechanism

Raw recursive harmonic mass rises sharply at the third generation, but repeated-root descendant mass falls:

```text
first retained:      0.272749423160
second recursive:    0.167467988429
third recursive:     0.013654746193.
```

The stored repeated-root term releases enough capacity to absorb the third-generation harmonic expansion.

---

## 3. Immediate-depth-tail potential

Define the immediate depth drop

```math
d_i(u)=\left\lfloor\log_2\frac{i}{u}\right\rfloor
```

and the immediate depth-four tail

```math
T_g
=
\sum_{(u,i)\in F_g\,:\,d_i(u)\ge4}\frac1u.
```

The second candidate is

```math
\boxed{
\Phi_g^{\mathrm{tail}}=H_g+4T_g.
}
```

Its approximate exact values are

| family | `H + 4T` |
|---|---:|
| first retained | 1.332747656385 |
| second recursive | 0.540816753141 |
| third recursive | 0.540106510240 |

The exact ratios satisfy

```math
\frac{40579}{100000}
<
\frac{\Phi_2^{\mathrm{tail}}}{\Phi_1^{\mathrm{tail}}}
<
\frac{405791}{1000000},
```

and

```math
\boxed{
\frac{499343}{500000}
<
\frac{\Phi_3^{\mathrm{tail}}}{\Phi_2^{\mathrm{tail}}}
<
\frac{998687}{1000000}.
}
```

It contracts by `0.1313%–0.1314%` on the difficult second transition. The margin is small but strictly positive in exact arithmetic.

This coordinate is path-aware and uses the same immediate-provenance information that separated the first recorded terminal-token collision.

---

## 4. Exact single-feature screen

With the coefficient of current harmonic mass fixed to one, eleven nonnegative single-feature corrections were tested against both recursive transitions.

Exactly four are feasible:

```text
immediate_tail_ge4_descendant_mass
root_occurrence_mass
root_repeat_descendant_mass
root_repeat_occurrence_mass.
```

The minimum coefficient for repeated-root descendant mass lies near `1.762615`, so coefficient `2` is a simple integer witness.

The minimum coefficient for the immediate depth-four tail lies near `3.989549`, so coefficient `4` is a simple integer witness.

The following single-coordinate corrections are infeasible:

```text
immediate_depth_charge
immediate_occurrence_mass
immediate_repeat_descendant_mass
immediate_repeat_occurrence_mass
root_depth_charge
root_tail_ge4_descendant_mass
root_tail_ge8_descendant_mass.
```

In particular, generic depth charge is insufficient; the successful tail coordinate depends on **immediate** provenance and a thresholded descendant-mass charge.

---

## 5. Interpretation

The third-generation expansion did not eliminate the retained-potential route. It eliminated raw harmonic mass as the sole recursive coordinate.

The repeated-root witness provides the first same-form finite potential that absorbs both recorded transitions:

```text
contraction transition -> contraction
raw expansion transition -> contraction after provenance reserve release.
```

The immediate-tail witness independently shows that a path-sensitive depth reserve can also work, though with a narrow second-transition margin.

Neither result proves a whole-tree Bellman inequality. Remaining requirements include:

1. test both coordinates on the fourth retained recursive generation;
2. prove or refute monotonicity under other complete deletion policies and retained quotients;
3. combine recursive potential with first-appearance terminal accounting;
4. control recreation of repeated-root or immediate-tail capacity across branches;
5. attach obstruction credit or extract an exact failing transition.

The next finite test should propagate the 14 third-generation recursive states and evaluate these two potentials before introducing additional coordinates.
