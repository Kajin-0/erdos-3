# Production-compatible economical activation row

## Status

State-independent one-generation composition of exact affine occurrence accounting, capacity-aware latent reserve matching, and source-weighted direct discharge of only the physical pairs actually exposed by the child family.

This row uses the raw affine coordinate

```math
H+J.
```

Its overlap residue contracts by at most one half at owner exponent one. This remains valid after correcting the middle-child scale to `L<=N/2` because the raw pair term has coefficient one.

The doubled future-production coordinate `H+2J` is different; its critical latent-latent residual may equal one full parent pair unit.

---

## 1. Economical physical pair set

Let

```math
P\subseteq[N,2N)
```

and let `F` be the physical first-appearance union of all retained current and recursive latent child resources.

Mark `F` unavailable before center/opposite reserve matching. Let `R_used` be the matched reserve union and define

```math
\boxed{U=\mathcal F\sqcup R_{\rm used}.}
```

Only pairs in `U` are activated. Unused pairs in the quadratic parent pair universe are never prepaid.

---

## 2. Exact affine occurrence identity

Define

```math
\operatorname{Child}_{\rm raw}(P)
=
\sum_i
\left(
H(S_{r_i}(Q_i))
+
\mathbf 1_{i\text{ recursive}}J(Q_i)
\right).
```

Let:

```text
C_current = one extra current occurrence for each current-latent repeat;
X_middle  = one original middle occurrence for each unmatched latent duplicate.
```

Exact owner and reserve accounting gives

```math
\boxed{
\operatorname{Child}_{\rm raw}(P)
=
J(U)
+
W(C_{\rm current})
+
W(X_{\rm middle}).
}
```

Every occurrence has one named role, and no pair outside `U` appears.

---

## 3. Source-weighted direct discharge

Let `E(P)` be the complete parent full-edge occurrence family, of mass

```math
W(\mathscr E(P))=\frac52\mathcal L_3(P).
```

Apply direct maximal-ambient discharge only to `U`. A locally completed pair consumes one deterministic parent edge occurrence. Let `E_free` be the unused edge-token family.

Let

```text
mu_dir_rec  = recursive outgoing source-owned occurrence measure;
mu_dir_term = terminal outgoing source-owned occurrence measure.
```

Every outgoing occurrence carries exact inherited source mass. The production-compatible identity is

```math
\boxed{
J(U)
+
W(\mathscr E_{\rm free})
=
\frac52\mathcal L_3(P)
+
W(\mu_{\rm dir}^{\rm rec})
+
W(\mu_{\rm dir}^{\rm term}).
}
```

No target is replaced by its full pair capacity.

---

## 4. Complete raw Bellman row

Combining the preceding identities gives

```math
\boxed{
\begin{aligned}
\operatorname{Child}_{\rm raw}(P)
+
W(\mathscr E_{\rm free})
={}&
\frac52\mathcal L_3(P)\\
&+
W(\mu_{\rm dir}^{\rm rec})
+
W(\mu_{\rm dir}^{\rm term})\\
&+
W(C_{\rm current})
+
W(X_{\rm middle}).
\end{aligned}
}
```

Dropping the nonnegative free-token term gives the weaker production-only inequality.

The intermediate pair energy has disappeared without activating unused parent pairs or regenerating local edge production.

---

## 5. Ownership audit

Every object has one role.

### Physical parent pair

```text
one child first appearance;
one matched latent reserve;
or unused.
```

### Parent edge occurrence

```text
pay one locally completed pair in U;
or remain in E_free.
```

### Repeated child occurrence

```text
current-latent repeat -> one extra current occurrence in C_current;
latent-latent repeat  -> one matched reserve or one middle occurrence in X_middle.
```

No anonymous collision term remains.

---

## 6. Correct raw owner-scale residue

The total-owner theorem gives

```math
c_f+\ell_f\le2,
```

so current-latent and latent-latent residuals are mutually exclusive for one physical resource.

Every child shell base satisfies

```math
L\le\frac N2.
```

### Current-latent profile

The extra current occurrence contributes to the raw owner-scale coordinate by at most

```math
\frac Lg
\le
\frac12\frac Ng.
```

### Latent-latent profile

If no reserve is matched, the extra raw middle pair occurrence contributes

```math
\frac Lg
\le
\frac12\frac Ng.
```

Therefore the raw `H+J` overlap residue satisfies

```math
\boxed{
\Theta_1^{H+J}(\text{overlap residue of }f)
\le
\frac12\Theta_1(f;N).
}
```

Every recursive raw overlap occurrence releases at least one dyadic owner level.

The former quarter-scale and two-level-release claims are withdrawn.

---

## 7. Difference from the doubled critical coordinate

The future-production potential uses

```math
H+2J.
```

In that coordinate, one latent occurrence at child half-scale costs

```math
2\frac{N/2}{g}=\frac Ng.
```

Two latent owners can therefore cost two parent units at exponent one, leaving residual one. This sharp obstruction is recorded in

```text
docs/coordinated-middle-half-scale-critical-no-go.md.
```

At exponent two, all current/latent owner multiplicity fits inside the original parent pair capacity.

---

## 8. Other output coordinates

### Direct recursive occurrences

The physical-gap moment is nonexpanding, and every first-appearance direct lineage terminates or recreates after finitely many identities.

### Terminal occurrences

Terminal direct and current occurrences leave the recursive occurrence ledger but retain source-indexed value.

### Free edge tokens

`E_free` remains on the left and may supply future local first appearance without regeneration.

---

## 9. Remaining global theorem

The raw economical pair-activation row is exact. The surviving tasks are:

1. telescope source-owned direct occurrence flow across actual owner shells;
2. preserve free edge-token capacity;
3. retain each recreation reserve once;
4. control terminal source value;
5. connect the valid raw `H+J` half-scale contraction to the collision-free quadratic `H+2J` row;
6. deduce summability of raw dyadic reciprocal densities.
