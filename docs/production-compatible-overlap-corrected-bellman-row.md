# Production-compatible economical activation Bellman row

## Status

State-independent one-generation composition of:

```text
exact affine child resource accounting;
current-latent separation;
total owner degree at most two;
capacity-aware latent reserve matching and recursive export;
production-compatible direct discharge of only the physical pairs actually used.
```

The intermediate pair potential is eliminated without activating the unused quadratic pair universe. The free full-edge occurrence-token term prevents local three-AP production from being spent twice.

---

## 1. Economical physical pair set

Let

```math
P\subseteq[N,2N)
```

be the parent root universe and let the point-disjoint retained affine child family have complete current-plus-recursive-latent occurrence family.

Let

```math
\mathcal F
```

be the physical first-appearance union of child resources. Before latent reserve matching, mark every pair in `F` unavailable. Let

```math
R_{\rm used}
```

be the matched center/opposite reserve union. Then

```math
\mathcal F\cap R_{\rm used}=\varnothing.
```

Define

```math
\boxed{U=\mathcal F\sqcup R_{\rm used}.}
```

Only pairs in `U` are needed to majorize the retained child family. Unused pairs in `binom(P,2)` are never activated.

---

## 2. Exact affine occurrence identity

Define

```math
\operatorname{Child}(P)
=
\sum_i
\left(
H(S_{r_i}(Q_i))
+
\mathbf 1_{i\text{ recursive}}J(Q_i)
\right).
```

Let

```text
C_current: one current occurrence for every current-latent overlap;
X_middle: one original middle occurrence for every unmatched latent duplicate.
```

The exact owner and reserve accounting gives

```math
\boxed{
\operatorname{Child}(P)
=
J(U)
+
W(C_{\rm current})
+
W(X_{\rm middle}).
}
```

This identity records only the physical pair capacity actually exposed.

---

## 3. Economical direct discharge

Let

```math
\mathscr E(P)
```

be the complete parent full-edge occurrence-token family, with

```math
W(\mathscr E(P))=\frac52\mathcal L_3(P).
```

Apply direct maximal-ambient discharge only to

```math
A=U.
```

For every pair in `U` completed locally by a parent three-AP, consume one deterministic edge occurrence. Let

```math
\mathscr E_{\rm free}
```

be the remaining parent edge-token family.

The production-compatible row gives

```math
\boxed{
J(U)
+
W(\mathscr E_{\rm free})
\le
\frac52\mathcal L_3(P)
+
J(E_{\rm new})
+
W(H_{\rm rec})
+
W(H_{\rm term}).
}
```

No pair outside `U` enters the discharge row.

---

## 4. Complete Bellman inequality

Add `E_free` to the exact affine identity and substitute the economical discharge estimate:

```math
\boxed{
\begin{aligned}
\operatorname{Child}(P)
+
W(\mathscr E_{\rm free})
\le{}&
\frac52\mathcal L_3(P)
+
J(E_{\rm new})\\
&+
W(H_{\rm rec})
+
W(H_{\rm term})\\
&+
W(C_{\rm current})
+
W(X_{\rm middle}).
\end{aligned}
}
```

The intermediate pair energy has disappeared, and no unused parent pair has been converted into a lineage.

Dropping the nonnegative free-token term gives the weaker production-only form

```math
\operatorname{Child}(P)
\le
\frac52\mathcal L_3(P)
+
J(E_{\rm new})
+
W(H_{\rm rec})
+
W(H_{\rm term})
+
W(C_{\rm current})
+
W(X_{\rm middle}).
```

---

## 5. Exact ownership audit

Every object has one role.

### Physical parent pair

```text
one child first appearance;
one matched latent reserve;
or unused.
```

The first two sets form the disjoint economical union `U`.

### Parent edge occurrence

```text
pay one locally completed pair in U;
or remain in E_free.
```

### Current-latent overlap

The extra current occurrence remains in `C_current`; its physical identity was already counted once in `F`.

### Unmatched latent duplicate

The original sponsor-owned middle occurrence remains in `X_middle`; no new occurrence is created.

Thus the final row contains neither anonymous collision nor unowned pair capacity.

---

## 6. Binary overlap residue

The total-owner theorem gives

```math
c_f+\ell_f\le2.
```

Therefore current-overlap and latent-latent export cannot occur simultaneously for one physical resource.

### Current-latent profile

One extra current occurrence survives. It lies in a retained child shell of base

```math
L\le\frac N2,
```

so

```math
\Theta_1(\text{current residue})
\le
\frac12\Theta_1(f;N).
```

### Backbone-middle latent profile

A matched reserve terminates the extra occurrence. If no reserve is matched, one original middle occurrence survives at scale

```math
L\le\frac N4,
```

so

```math
\Theta_1(\text{middle residue})
\le
\frac14\Theta_1(f;N).
```

The cases are exclusive. Hence

```math
\boxed{
\Theta_1(\text{complete overlap residue of }f)
\le
\frac12\Theta_1(f;N).
}
```

At raw pair weight, the current case releases at least one dyadic owner level and the middle case releases at least two.

---

## 7. Other output coordinates

### New pair union

`E_new` enters the occurrence-owned direct pair-lineage ledger. Physical dyadic gap never increases, and every first-appearance lineage terminates or recreates in finite time.

### Recursive heavy output

`H_rec` contracts physical dyadic gap by at least one level, or by at least two levels in the outer role.

### Terminal output

`H_term` and terminal members of `C_current` leave the recursive ledger.

### Free edge tokens

`E_free` remains on the left. It is production capacity not consumed by economical local pair payment.

---

## 8. Significance

The earlier pair-energy program faced two distinct losses:

```text
prepaying the complete quadratic pair universe;
reusing parent three-AP edge production for local payment and branching.
```

The economical row removes both:

```text
only U is activated;
used local edge occurrences are removed from E_free;
root overlap creates at most one half-scale residual occurrence.
```

This is the local economical pair-activation theorem required by the affine Bellman architecture.

---

## 9. Remaining global theorem

The local pair-energy entry and latent-activation problems are closed. The surviving whole-tree tasks are:

1. pack `E_new` across production owners and merge physical recreations;
2. sum finite direct pair lineages globally rather than pathwise;
3. control recreation of terminal current and heavy sinks;
4. combine the exact `7/4` first-appearance depth release with the sharp `1/2` overlap contraction;
5. preserve the free-edge occurrence ledger across generations;
6. deduce dyadic reciprocal-density summability.

The active obstruction is global first-appearance and recreation packing, not local pair activation.
