# Reserve pseudoforest small-box certificate

## Status

Exact exhaustive finite result for the actual lexicographic coordinated-deletion schedule followed by the canonical maximum-harmonic point-disjoint retained quotient.

It is not a proof that every reserve graph is a pseudoforest.

---

## 1. Search domain

The verifier enumerates every subset

```math
P\subseteq[1,16]
```

with at least three points, rejects every parent containing a four-term arithmetic progression, constructs the exact retained child family, and classifies repeated recursive latent parent-pair resources.

For every duplicated latent resource it checks the universal structural conclusions:

```text
latent owner degree at most two;
one backbone owner and one middle-fiber owner;
center/opposite reserve pairs remain in the parent;
reserve pairs preserve the demand gap exactly.
```

It then constructs the center/opposite reserve multigraph and computes every component cycle rank.

---

## 2. Exact result

The exhaustive profile is:

```text
four-AP-free parents             22,464
parents with latent reuse             0
total duplicated latent demands       0
maximum duplicated demands             0
cyclic reserve components              0
rank-two reserve components            0
maximum reserve cycle rank             0
```

The recorded payload hash is

```text
dc6d8d933eabbdbde4073276f980a92a7026770e56d3c34ce7108a57ba10db00
```

The exact certificate is stored at

```text
data/reserve_pseudoforest_endpoint16_2026-07-16.json
```

and regenerated by

```text
src/search_lexicographic_reserve_pseudoforest_small_box.py 16 OUTPUT.json
```

---

## 3. Interpretation

The result is stronger than a pseudoforest check on this interval: no retained parent pair has two recursive latent owners at all.

However, the first known policy-compatible latent-reuse example has span `34`, so `[1,16]` lies below the first observed obstruction scale. The certificate therefore establishes a verified baseline and validates the search implementation, but it is not substantive evidence that the global reserve graph must be a pseudoforest.

The active question remains:

```math
\boxed{
\text{Can an actual retained reserve component have cycle rank at least two?}
}
```

A positive example would identify the exact unmatched latent-reserve defect. A proof of impossibility would close the remaining one-generation pair-activation allocation.
