name: Record CL-083 affine pair tokens

on:
  push:
    branches:
      - main
    paths:
      - src/patch_cl083_affine_pair_tokens.py
      - .github/workflows/record-cl083-affine-pair-tokens.yml

permissions:
  contents: write

concurrency:
  group: probe-result-writers-main
  cancel-in-progress: false

jobs:
  record:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch authoritative documents
        run: |
          python3 -m py_compile src/patch_cl083_affine_pair_tokens.py
          python3 src/patch_cl083_affine_pair_tokens.py
      - name: Commit update
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/certainty-ledger.md docs/current-proof-program.md docs/research-decision-history.md docs/terminal-sink-first-appearance-ledger.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Record CL-083 affine pair-token theorem [skip ci]"
          git fetch origin main
          if ! git rebase origin/main; then
            git rebase --abort || true
            exit 1
          fi
          git push origin HEAD:main
